DROP TABLE blobclob_test CASCADE CONSTRAINTS PURGE;
/

CREATE TABLE blobclob_test (
    id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    name VARCHAR2(100),
    binary_data BLOB,
    text_data CLOB,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
/
