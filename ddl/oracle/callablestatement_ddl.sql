DROP TABLE callablestatement_test CASCADE CONSTRAINTS PURGE;
/

CREATE TABLE callablestatement_test (
    id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    name VARCHAR2(100) NOT NULL,
    value NUMBER DEFAULT 0,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
/

BEGIN EXECUTE IMMEDIATE 'DROP FUNCTION jt_add_numbers'; EXCEPTION WHEN OTHERS THEN NULL; END;
/
CREATE OR REPLACE FUNCTION jt_add_numbers(a IN NUMBER, b IN NUMBER) RETURN NUMBER IS
BEGIN
    RETURN a + b;
END;
/

BEGIN EXECUTE IMMEDIATE 'DROP FUNCTION jt_get_name_by_id'; EXCEPTION WHEN OTHERS THEN NULL; END;
/
CREATE OR REPLACE FUNCTION jt_get_name_by_id(p_id IN NUMBER) RETURN VARCHAR2 IS
    v_name VARCHAR2(100);
BEGIN
    SELECT name INTO v_name FROM callablestatement_test WHERE id = p_id;
    RETURN v_name;
END;
/

BEGIN EXECUTE IMMEDIATE 'DROP FUNCTION jt_multiply_numbers'; EXCEPTION WHEN OTHERS THEN NULL; END;
/
CREATE OR REPLACE FUNCTION jt_multiply_numbers(a IN NUMBER, b IN NUMBER) RETURN NUMBER IS
BEGIN
    RETURN a * b;
END;
/

BEGIN EXECUTE IMMEDIATE 'DROP FUNCTION jt_get_count'; EXCEPTION WHEN OTHERS THEN NULL; END;
/
CREATE OR REPLACE FUNCTION jt_get_count RETURN NUMBER IS
    v_cnt NUMBER;
BEGIN
    SELECT COUNT(*) INTO v_cnt FROM callablestatement_test;
    RETURN v_cnt;
END;
/
